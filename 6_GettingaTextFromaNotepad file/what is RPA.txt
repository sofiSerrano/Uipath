What is rpa?

Our process will assume that the file is closed at runtime, so we’ll drag an Open Application activity. We will indicate the Notepad application on the screen and rename the activity accordingly. We still need to add the name of the file which we want to open so, in the Properties panel, let’s update the Arguments parameter with the name of the Notepad file we want to retrieve the text from, in our case, “What is RPA”. 